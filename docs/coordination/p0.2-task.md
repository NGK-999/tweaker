# Task P0.2 — bloqueadores da revisão do PR #2

**Task ID:** P0.2-BLOCKERS  
**Status:** READY FOR RE-REVIEW (implementado; merge ainda bloqueado)  
**PR:** https://github.com/NGK-999/tweaker/pull/2  
**Branch:** `integration/p0-demo-outcome-feedback`  
**Merge em main:** **PROIBIDO** até nova revisão humana aprovada  
**Stash:** **NÃO** aplicar `wip-pre-p0-integration-*`

## Objetivo

Corrigir somente os bloqueadores da revisão do PR #2. Escopo restrito. Sem refatoração completa. Sem merge.

## Escopo permitido

| Área | Arquivos típicos |
|------|------------------|
| Outcome / cancel / ledger | `src/Services/MutationExecutor.cs`, `src/Services/BackupService.cs` (hook de teste mínimo) |
| Classificador | `src/Infrastructure/CommandClassifier.cs` |
| Self-test | `src/App/DemoSafetySelfTest.cs` |
| Snackbar / Catalog | `src/UI/Wpf/MainWindow.xaml.cs`, `Views/CatalogView.*`, `ViewModels/CatalogViewModel.cs` |
| Docs | `docs/coordination/*` desta tarefa |

## Fora de escopo

- Merge em `main`
- Aplicar stash WIP
- Reescrever `CommandRunner` / `TweakService` / dual stack Catalog vs Auto
- Restaurar classificação Snackbar por substring
- Expandir allowlist de leitura além do necessário para os casos adversarial
- Contratos `Models`/`Contracts` congelados

---

## Acceptance criteria

### 1. Outcome isolado por operação

- [x] `LastOutcome` limpo no início de cada `RunAsync`
- [x] Outcome local por execução; `LastOutcome` só ao terminar
- [x] Sem `??=` que preserve outcome anterior após rebuild final
- [x] Teste: mesma instância → 1ª COMPLETED → 2ª FAILED → `LastOutcome` só da segunda

### 2. Cancelamento

- [x] `OperationCanceledException` **não** vira conclusão normal
- [x] OCE em Validate/Snapshot/Execute/Verify classificada como Cancelled
- [x] Propagação preservada (rethrow) enquanto consumidores recebem logs
- [x] OCE **não** capturada só no `catch (Exception)`
- [x] Teste: cancel no action **e** cancel dentro de `command.Execute()` → Kind Cancelled + exceção propaga

### 3. Ledger e outcome final

- [x] Outcome definitivo montado **depois** da tentativa de `CommitMutationSession`
- [x] Se persistência do ledger falhar → outcome reflete `RollbackRequired`
- [x] Não usar outcome pré-`finally` como definitivo sem reconstrução
- [x] Teste: commit que falha → `LastOutcome.RollbackRequired == true`

### 4. CommandClassifier adversarial

- [x] Não confiar só no basename
- [x] Em Demo/Unknown: ReadOnly só para executáveis confiáveis/canônicos
- [x] `C:\Temp\powercfg.exe /list` bloqueado
- [x] Token mutador vence; misturas → Unknown (bloqueado em Demo)
- [x] Testes: homônimo fora System32; DISM misto; netsh misto; shell wrappers; aspas/casing; bare powercfg OK

### 5. Snackbar global

- [x] Call sites críticos com `SnackbarKind` explícito
- [x] Sem heurística por substring
- [x] Fluxos fora do Catálogo preservam feedback semântico

### Correções adicionais

- [x] Análise inicial do `CatalogView` no `Loaded` (após wiring no MainWindow)
- [x] Sem `ex.Message` como mensagem principal no Catalog
- [x] Assert real no outcome rico (timestamps / Steps not null)

---

## Verificação obrigatória

```text
Build Release: PASS
demo-self-test: PASS
gaming-fps-probe-self-test: PASS
catalog-feedback-self-test: PASS
smoke GUI (--demo): PASS
órfãos ApexTweaker: 0
```

## Decisão de merge

```text
PR #2: aguarda re-revisão humana
Merge em main: NÃO
```

## Prompt / routing

- Prompt: `docs/coordination/prompts/P0.2-BLOCKERS.md`
- Routing: `docs/coordination/prompts/P0.2-BLOCKERS-routing.md`
