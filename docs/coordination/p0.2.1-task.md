# Task P0.2.1 — resolução canônica + timeout/cancel em estágio

**Task ID:** P0.2.1-CANONICAL-TIMEOUT  
**Status:** READY FOR RE-REVIEW  
**PR:** https://github.com/NGK-999/tweaker/pull/2  
**Branch:** `integration/p0-demo-outcome-feedback`  
**Base HEAD revisado:** `e0af93e`  
**Merge em main:** **PROIBIDO** até nova aprovação  
**Stash:** **NÃO** aplicar

## Bloqueadores desta rodada

1. Bare names (`powercfg`) confiáveis sem resolver o binário real → CommandRunner executa caminho canônico System32.
2. `TimeoutException` em estágio de comando engolida por `catch (Exception)` em `Execute`.
3. Cancel durante `Execute` sem `RollbackRequired`.
4. Catalog descarta `ex` sem registrar diagnóstico técnico.

## Acceptance criteria

- [x] `TrustedCommandResolution Resolve(string)` com `IsTrusted`, `CanonicalPath`, `ExecutableName`
- [x] Demo/Unknown: ReadOnly só com caminho canônico confiável; execução usa `CanonicalPath`
- [x] Bare tools → System32/SysWOW64 (sem PATH/cwd)
- [x] `TimeoutException` relançada de `Execute` até `RunAsync` → Kind TimedOut + RollbackRequired
- [x] Cancel em estágio Execute → RollbackRequired
- [x] Catalog: `ex.ToString()` em `LastTechnicalDiagnostic` + Trace; UI amigável
- [x] Testes + build/self-tests/smoke PASS

## Verificação

```text
build Release: PASS
demo-self-test: PASS
catalog-feedback-self-test: PASS
gaming-fps-probe-self-test: PASS
smoke GUI --demo: PASS
órfãos: 0
```

## Fora de escopo

Merge, stash, refatoração ampla, CI Actions (dívida de maturidade).
