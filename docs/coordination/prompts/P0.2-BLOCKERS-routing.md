# P0.2-BLOCKERS — routing

| Campo | Valor |
|-------|--------|
| Task | P0.2-BLOCKERS |
| Executor | cursor-orchestrator (já na branch de integração) |
| Fallback BE | Codex `gpt-5.6-terra` — só `MutationExecutor` / `CommandClassifier` / `DemoSafetySelfTest` |
| Fallback FE | Claude Sonnet — só Snackbar/CatalogView se orquestrador pedir |
| Worktree | `C:\projetos\Apextweaker` (integration branch) — **não** abrir worktree paralelo sem necessidade |
| Merge | **bloqueado** |
| Stash | **não aplicar** |

## Ownership

- BE: `MutationExecutor`, `CommandClassifier`, `DemoSafetySelfTest`, mínimo em `BackupService` se teste de ledger exigir
- FE: `MainWindow` SetStatus críticos, `CatalogView` Loaded, `CatalogViewModel` copy
- Congelado: Models/Contracts, redesign CommandRunner, dual stack otimização

## Gate antes de pedir re-revisão

1. Todos AC do `p0.2-task.md` marcados  
2. Self-tests PASS  
3. Smoke GUI + sem órfãos  
4. Diff restrito  
5. Humano re-revisa PR #2  
