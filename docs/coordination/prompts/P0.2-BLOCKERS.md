# P0.2-BLOCKERS — prompt de execução

Você está corrigindo **somente** os bloqueadores da revisão do PR #2 do ApexTweaker.

## Contexto

- Branch: `integration/p0-demo-outcome-feedback`
- PR: https://github.com/NGK-999/tweaker/pull/2
- **Não** mergear em `main`
- **Não** aplicar stash `wip-pre-p0-integration-*`
- Há implementação parcial em `cb6a45d` — complete gaps; não redesenhar

## Regras

1. Use `graphify query` antes de explorar em massa.
2. Diff mínimo; arquivos listados em `docs/coordination/p0.2-task.md`.
3. Responda em português no handoff.
4. Após editar código: `graphify update .`
5. Demo/Unknown continuam fail-closed.

## Trabalho

Implemente/complete os 5 bloqueadores + correções adicionais do `p0.2-task.md`.

Prioridade de falhas:

1. LastOutcome isolado  
2. Cancelamento propaga e classifica Cancelled  
3. Outcome após ledger  
4. Classificador adversarial  
5. Snackbar tipado nos call sites críticos  

## Testes mínimos a adicionar/garantir

```text
mesma MutationExecutor: COMPLETED depois FAILED → LastOutcome = segunda
OCE no action → rethrow + Kind Cancelled
OCE em command.Execute → rethrow + Kind Cancelled
CommitMutationSession falha → RollbackRequired no outcome
C:\Temp\powercfg.exe /list → bloqueado
dism ... /get-features ... /enable-feature → bloqueado
netsh ... show ... set ... → bloqueado
cmd/powershell wrappers → Mutation/bloqueado
aspas + casing em caminhos → comportamento seguro
powercfg /list bare → ReadOnly permitido
Steps assert real (não Count >= 0)
```

## Verificação

Build Release + três self-tests + smoke GUI sem órfãos.

## Entrega

- Commit na branch do PR (sem squash destrutivo da história)
- Atualizar `docs/coordination/integration-status-p0.md` e checkboxes do `p0.2-task.md`
- **Não** mergear
