# N/A — OpenAPI

Este projeto **não possui API HTTP**.

O contrato vivo está em:

- `docs/contracts/api-contract.md`
- `src/Models/*.cs`
- superfícies públicas de `src/Services` e `src/Minecraft`

Não criar `api.openapi.yaml` até existir um host HTTP real (não planejado na Fase 1–6).
