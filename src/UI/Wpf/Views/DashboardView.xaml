<UserControl x:Class="ApexTweaker.UI.Wpf.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2A2A2A" />
            <Setter Property="BorderBrush" Value="#3A3A3C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="20" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="52" />
            <Setter Property="Background" Value="#00B4D8" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="RootBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="RootBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="#38383A" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0">
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="One-Click Auto-Tuning"
                               Foreground="#00B4D8"
                               FontSize="24"
                               FontWeight="Bold" />
                    <TextBlock Margin="0,8,0,16"
                               Foreground="#A1A1AA"
                               FontSize="14"
                               Text="O shell WPF assume o fluxo principal. O backend transacional permanece intacto." />
                    <Button x:Name="AutoOptimizeButton"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Content="&#x26A1; OTIMIZAR SISTEMA AO M&#x00C1;XIMO"
                            Click="AutoOptimizeButton_OnClick" />
                </StackPanel>
            </Border>

            <Grid Margin="0,16,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Seguran&#x00E7;a do Sistema"
                                   Foreground="#00B4D8"
                                   FontSize="20"
                                   FontWeight="Bold" />
                        <TextBlock Margin="0,8,0,14"
                                   Foreground="#A1A1AA"
                                   Text="Crie um ponto de restaura&#x00E7;&#x00E3;o antes de aplicar pacotes agressivos." />
                        <Button x:Name="RestorePointButton"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Content="Criar ponto de restaura&#x00E7;&#x00E3;o"
                                Click="RestorePointButton_OnClick" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Resumo"
                                   Foreground="#00B4D8"
                                   FontSize="20"
                                   FontWeight="Bold" />
                        <TextBlock x:Name="SummaryText"
                                   Margin="0,12,0,0"
                                   Foreground="#E4E4E7"
                                   FontSize="14"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>